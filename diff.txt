diff --git a/app/controllers/gifts_controller.rb b/app/controllers/gifts_controller.rb
index bcb3a05..1bed44d 100644
--- a/app/controllers/gifts_controller.rb
+++ b/app/controllers/gifts_controller.rb
@@ -1,45 +1,27 @@
 class GiftsController < ApplicationController
 
-  def self.index_title
-    if @giver && @recipient
-      index_title = "Gifts from #{@giver.name} to #{@recipient.name}"
-    elsif @giver == current_user && @recipient
-      index_title = "gifts that you are giving to #{@recipient.name}"
-    elsif @giver == current_user
-      index_title = 'Gifts that you are giving'
-    elsif @wish
-      index_title = "Gifts fulfilling #{@wish.friend.name}'s wish for #{@wish.title}"
-    elsif @giver
-      index_title = "Gifts that #{@giver.name} is giving"
-    elsif @recipient
-      index_title = "Gifts that #{@recipient.name} is receiving"
-    else
-      index_title = "All gifts"
-    end
-  end
-
   def show
     @gift = Gift.find_by(id: params[:id])
     if @gift.nil?
       render status: 404, file: "#{Rails.root}/public/404.html" and return
-    elsif @gift.recipient == current_user # to do: make sure this works
+    elsif @gift.recipient == @current_user # to do: make sure this works
       render status: 403, file: "#{Rails.root}/public/403.html" and return
       # to do: maybe have something other than an error here?
     end
   end
 
   def index
-    if params[:friend_id].to_i == current_user.id.to_i
+    if params[:friend_id].to_i == @current_user.id.to_i
       render status: 403, file: "#{Rails.root}/public/403.html" and return
       # to do: maybe have something other than an error here?
     elsif params[:friend_id]
       @friend = Friend.find_by(id: params[:friend_id])
       @wish = Wish.find_by(id: params[:wish_id])
       @receiving_gifts = Gift.where(recipient: @friend, year: Time.christmas_year).reject do |f|
-        f.recipient_id == current_user.id
+        f.recipient_id == @current_user.id
       end
       @giving_gifts = Gift.where(giver: @friend, year: Time.christmas_year).reject do |f|
-        f.recipient_id == current_user.id
+        f.recipient_id == @current_user.id
       end
       render 'friend_gifts' and return # to do: make this
     else
@@ -55,16 +37,16 @@ class GiftsController < ApplicationController
     if @wish && @recipient && @wish.friend != @recipient
       render status: 404, file: "#{Rails.root}/public/404.html" and return
     end
-    @giver = current_user
+    @giver = @current_user
   end
 
   def create
     @gift = Gift.new(gift_params)
-    if (@gift.recipient != current_user) && @gift.save
+    if (@gift.recipient != @current_user) && @gift.save
       flash[:success] = 'Gift saved'
       redirect_to Friend.find_by(id: @gift.recipient)
       # to do: customize this by the incoming url
-    elsif @gift.recipient == current_user
+    elsif @gift.recipient == @current_user
       flash[:danger] = "You can't give a gift to yourself"
       render 'give'
       # to do: customize this by the incoming url
@@ -77,7 +59,7 @@ class GiftsController < ApplicationController
 
   def edit
     @gift = Gift.find(params[:gift_id])
-    unless @gift.giver == current_user
+    unless @gift.giver == @current_user
       flash[:danger] = 'You can only edit gifts that you are giving'
       redirect_to(@gift)
       # to do: customize this by the incoming url
@@ -103,7 +85,7 @@ class GiftsController < ApplicationController
       render status: 404, file: "#{Rails.root}/public/404.html"
       return
     end
-    if @gift.giver == current_user
+    if @gift.giver == @current_user
      if @gift.destroy
        flash[:success] = "Gift deleted"
      else
@@ -112,7 +94,7 @@ class GiftsController < ApplicationController
    else
      flash[:danger] = "You may only delete gifts that you are giving"
    end
-   redirect_to(current_user) # to do: customize this by the incoming url
+   redirect_to(@current_user) # to do: customize this by the incoming url
   end
 
   def from_to
@@ -124,6 +106,8 @@ class GiftsController < ApplicationController
       @giver = Friend.find_by(id: params[:giver_id])
       @recipient_id = Friend.find_by(id: params[:recipient_id])
       @gifts = Gift.where(giver_id: params[:giver_id], recipient_id: params[:recipient_id])
+      @foo = params[:recipient_id]
+      @bar = current_user.id.to_i
       render 'index' and return
     end
   end
diff --git a/app/controllers/wishes_controller.rb b/app/controllers/wishes_controller.rb
index b445f90..cf4b09f 100644
--- a/app/controllers/wishes_controller.rb
+++ b/app/controllers/wishes_controller.rb
@@ -3,36 +3,9 @@ class WishesController < ApplicationController
   respond_to :html, :json
 
   def new
-    @wish = Wish.new(friend_id: current_user.id, year: Time.christmas_year)
   end
 
   def create
-    @wish = Wish.new(wish_params)
-    if @wish.save!
-      flash[:success] = 'Wish created successfully'
-      redirect_to friend_wishes_path(current_user)
-    else
-      flash[:failure] = 'Something went wrong and the wish was not created successfully'
-      redirect_to 'new'
-    end
-  end
-
-  def edit
-    @wish = Wish.find_by(id: params[:id])
-    unless @wish.friend == current_user
-      render status: 403, file: "#{Rails.root}/public/403.html" and return
-    end
-  end
-
-  def update
-    @wish = Wish.find_by(id: params[:id])
-    if @wish.update_attributes!(wish_params)
-      flash[:success] = 'Wish created successfully'
-      redirect_to @wish
-    else
-      flash[:failure] = 'Something went wrong and the wish was not edited successfully'
-      redirect_to 'new'
-    end
   end
 
   def show
@@ -47,20 +20,6 @@ class WishesController < ApplicationController
     @wishes = (@friend) ? Wish.where(friend_id: @friend.id) : Wish.all
   end
 
-  def destroy
-    @wish = Wish.find_by(id: params[:id])
-    if @wish && @wish.friend == current_user
-      if @wish.destroy!
-       flash[:success] = "Wish deleted"
-      else
-        flash[:danger] = "Wish not deleted, something went wrong"
-      end
-    else
-      flash[:danger] = "You may only delete wishes that are yours"
-    end
-    redirect_to friend_wishes_path(current_user) # to do: customize this by the incoming url
-  end
-
   def friend_wishes_ajax
     friend_id = params[:id]
     @friend_wishes = Wish.where(year: Time.christmas_year, friend_id: friend_id)
@@ -69,10 +28,4 @@ class WishesController < ApplicationController
     end
   end
 
-  private
-
-  def wish_params
-    params.require(:wish).permit(:title, :description, :friend_id, :year)
-  end
-
 end
diff --git a/app/helpers/gifts_helper.rb b/app/helpers/gifts_helper.rb
index 4a6f5a4..19b9d2b 100644
--- a/app/helpers/gifts_helper.rb
+++ b/app/helpers/gifts_helper.rb
@@ -1,19 +1,3 @@
 module GiftsHelper
-  def self.index_title
-    if @giver && @recipient
-      index_title = "Gifts from #{@giver.name} to #{@recipient.name}"
-    elsif @giver == current_user && @recipient
-      index_title = "gifts that you are giving to #{@recipient.name}"
-    elsif @giver == current_user
-      index_title = 'Gifts that you are giving'
-    elsif @wish
-      index_title = "Gifts fulfilling #{@wish.friend.name}'s wish for #{@wish.title}"
-    elsif @giver
-      index_title = "Gifts that #{@giver.name} is giving"
-    elsif @recipient
-      index_title = "Gifts that #{@recipient.name} is receiving"
-    else
-      index_title = "All gifts"
-    end
-  end
+  
 end
diff --git a/app/helpers/sessions_helper.rb b/app/helpers/sessions_helper.rb
index 5ac0359..62cb333 100644
--- a/app/helpers/sessions_helper.rb
+++ b/app/helpers/sessions_helper.rb
@@ -9,7 +9,7 @@ module SessionsHelper
   end
 
   def logged_in?
-    !@current_user.nil?
+    !current_user.nil?
   end
 
   def log_out
diff --git a/app/views/friends/show.html.erb b/app/views/friends/show.html.erb
index 621f685..11c5d8b 100644
--- a/app/views/friends/show.html.erb
+++ b/app/views/friends/show.html.erb
@@ -23,15 +23,14 @@
   </tbody>
 </table>
 
-<% unless @friend == current_user %>
-  <h2><%= link_to "All gifts that #{@friend.name} is receiving", friend_gifts_path(@friend) %>
-<% end %>
+<h2><%= link_to "All gifts that #{@friend.name} is receiving", friend_gifts_path(@friend) %>
 
 <br>
 <h2>Generation</h2>
 <%= link_to @friend.generation.name, generation_friend_path(@friend) %>
 <br>
-<%= link_to "#{@friend.name}'s wishlist", friend_wishes_path(@friend) %>
-<% unless @friend.id == current_user.id %>
+<% if @friend.id == current_user.id %>
+  <%= link_to "Add gift to wishlist", new_wish_path %>
+<% else %>
   <%= link_to "Give this person a gift", new_gift_path %>
 <% end %>
diff --git a/app/views/gifts/edit.html.erb b/app/views/gifts/edit.html.erb
deleted file mode 100644
index 9b90392..0000000
--- a/app/views/gifts/edit.html.erb
+++ /dev/null
@@ -1,51 +0,0 @@
-<% provide(:title, 'New gift') %>
-
-<div class="row">
-  <div class="col-md-6 col-md-offset-3">
-    <%= form_for(@gift) do |f| %>
-
-      <%= f.label :title, " Title" %>
-      <%= f.text_field :title, class: 'form-control' %><br>
-
-      <%= f.label :description, "A longer description of your gift with any relevant details, including URLs, ISBNs, etc" %>
-      <%= f.text_area :description, :cols => "10", :rows => "10", class: 'form-control' %><br>
-
-
-      <%= f.label :recipient_id, 'Who is this gift for?' %>
-      <%= f.collection_select(:recipient_id, Friend.all, :id, :name,
-        include_blank: false, id: 'gift_recipient_select', selected: @gift.recipient) %>
-
-      <%= f.label :wish_id, "What wish, if any, is this gift fulfilling?" %>
-      <%= f.collection_select(:wish_id, @gift.recipient.wishes, :id,
-        :title, include_blank: true, id: 'gift_wish_select', selected: @gift.wish) %>
-
-      <h4>Have you purcased and/or sent this gift yet?</h4>
-      <%= f.label :purchase_status_intend, "I have not yet purchased this item, but I intend to" %>
-      <%= f.radio_button :purchase_status, "intend", checked: true %>
-      <%= f.label :purchase_status_purchased, "I have already purchased this item, but have not sent it out yet" %>
-      <%= f.radio_button :purchase_status, "purchased" %>
-      <%= f.label :purchase_status_sent, "I have already purchased and sent this item" %>
-      <%= f.radio_button :purchase_status, "sent" %>
-
-      <%= f.hidden_field :giver_id, value: current_user.id %>
-      <%= f.hidden_field :year, value: Time.christmas_year %>
-
-      <%= f.submit "Give gift", class: "btn btn-primary" %>
-      <%= link_to "Cancel", friend_path(id: current_user.id) %>
-    <% end %>
-  </div>
-  <script type="text/javascript">
-  var $url_arr = window.location.href.split("/");
-  var $root_url = $url_arr[0] + "//" + $url_arr[2];
-  $("select#gift_recipient_id").change(function(){
-    var full_url = $root_url + "/friend_wishes_ajax" + "/" + $("select#gift_recipient_id option:selected:first").val() + ".json";
-    $('select#gift_wish_id option:gt(0)').remove();
-    $.getJSON( full_url, function( data ) {
-      $.each( data, function( ind, val ) {
-        $("select#gift_wish_id").append($("<option></option>")
-          .attr("value", val["id"]).text(val["title"]));
-      });
-    });
-  });
-  </script>
-</div>
diff --git a/app/views/gifts/index.html.erb b/app/views/gifts/index.html.erb
index 9de5ac2..658d4bb 100644
--- a/app/views/gifts/index.html.erb
+++ b/app/views/gifts/index.html.erb
@@ -1,6 +1,22 @@
 <% controller.redirect_to '/error/403' if @recipient == current_user %>
 
-<% provide(:title, GiftsController.index_title) %>
+<% index_title = if @giver && @recipient %>
+  <% "Gifts from #{@giver.name} to #{@recipient.name}" %>
+<% elsif @giver == current_user && @recipient %>
+  <% "gifts that you are giving to #{@recipient.name}" %>
+<% elsif @giver == current_user %>
+  <% 'Gifts that you are giving' %>
+<% elsif @wish %>
+  <% "Gifts fulfilling #{@wish.friend.name}'s wish for #{@wish.title}" %>
+<% elsif @giver %>
+  <% "Gifts that #{@giver.name} is giving" %>
+<% elsif @recipient %>
+  <% "Gifts that #{@recipient.name} is receiving" %>
+<% else %>
+  <% "All gifts" %>
+<% end %>
+
+<% provide(:title, index_title) %>
 
 <h1>index_title</h1>
 <%# to do: improve this %>
diff --git a/app/views/gifts/new.html.erb b/app/views/gifts/new.html.erb
index 6d68e2d..a516f8f 100644
--- a/app/views/gifts/new.html.erb
+++ b/app/views/gifts/new.html.erb
@@ -5,10 +5,10 @@
     <%= form_for(@gift) do |f| %>
 
       <%= f.label :title, " Title" %>
-      <%= f.text_field :title, class: 'form-control'  %><br>
+      <%= f.text_field :title %><br>
 
       <%= f.label :description, "A longer description of your gift with any relevant details, including URLs, ISBNs, etc" %>
-      <%= f.text_area :description, :cols => "10", :rows => "10", class: 'form-control'  %><br>
+      <%= f.text_area :description, :cols => "10", :rows => "10" %><br>
 
       <% if @recipient %>
         <%= f.hidden_field :recipient_id, value: @recipient.id %>
@@ -19,18 +19,12 @@
       <% end %><br>
 
       <h4>Have you purcased and/or sent this gift yet?</h4>
-      <%= f.label :purchase_status_intend,
-        "I have not yet purchased this item, but I intend to" %>
-      <%= f.radio_button :purchase_status, "intend",
-        checked: @gift.purchase_status == 'intend' %>
-      <%= f.label :purchase_status_purchased,
-        "I have already purchased this item, but have not sent it out yet" %>
-      <%= f.radio_button :purchase_status, "purchased",
-        checked: @gift.purchase_status == 'purchased' %>
-      <%= f.label :purchase_status_sent,
-        "I have already purchased and sent this item" %>
-      <%= f.radio_button :purchase_status, "sent",
-        checked: @gift.purchase_status == 'sent' %>
+      <%= f.label :purchase_status_intend, "I have not yet purchased this item, but I intend to" %>
+      <%= f.radio_button :purchase_status, "intend", checked: true %>
+      <%= f.label :purchase_status_purchased, "I have already purchased this item, but have not sent it out yet" %>
+      <%= f.radio_button :purchase_status, "purchased" %>
+      <%= f.label :purchase_status_sent, "I have already purchased and sent this item" %>
+      <%= f.radio_button :purchase_status, "sent" %>
 
       <%= f.hidden_field :giver_id, value: current_user.id %>
       <%= f.hidden_field :year, value: Time.christmas_year %>
diff --git a/app/views/gifts/show.html.erb b/app/views/gifts/show.html.erb
index 53eb41e..ee9675d 100644
--- a/app/views/gifts/show.html.erb
+++ b/app/views/gifts/show.html.erb
@@ -8,8 +8,6 @@
   <%= @gift.description %>
   <h4>Recipient</h4>
   <%= @gift.recipient.name %>
-  <h4>Giver</h4>
-  <%= @gift.giver.name %>
   <% if @gift.wish %>
     <h4>Fulfillment of wish:</h4>
     <%= link_to @gift.wish.title, wish_path(@gift.wish) %>
diff --git a/app/views/wishes/edit.html.erb b/app/views/wishes/edit.html.erb
deleted file mode 100644
index 2687e62..0000000
--- a/app/views/wishes/edit.html.erb
+++ /dev/null
@@ -1,19 +0,0 @@
-<% provide title: "Edit wish for #{@wish.title}" %>
-
-<div class="row">
-  <div class="col-md-6 col-md-offset-3">
-    <%= form_for(@wish) do |f| %>
-      <%= f.label :title, 'Title' %>
-      <%= f.text_field :title, class: 'form-control'  %><br>
-
-      <%= f.label :description, 'A longer description of your gift with any relevant details, including URLs, ISBNs, etc' %>
-      <%= f.text_area :description, cols: '10', rows: '10', class: 'form-control'  %><br>
-
-      <%= f.hidden_field :friend_id, value: current_user.id %>
-      <%= f.hidden_field :year, value: Time.christmas_year %>
-
-      <%= f.submit 'Save changes', class: 'btn-btn-primary' %>
-      <%= link_to 'Cancel', :back %>
-    <% end %>
-  </div>
-</div>
diff --git a/app/views/wishes/index.html.erb b/app/views/wishes/index.html.erb
index dd29a5c..a525973 100644
--- a/app/views/wishes/index.html.erb
+++ b/app/views/wishes/index.html.erb
@@ -17,7 +17,7 @@
       <% end %>
       <td colspan="3"><%= wish.description %></td>
       <% if current_user == wish.friend %>
-        <td colspan="3">You'll just have to wait until Christmas to find out!</td>
+        <td colspan="3">You'll just have to wait until Christmas!</td>
       <% elsif wish.gifts.any? %>
         <td colspan="3"><%= render partial: 'gift_title_links', locals: {wish: wish} %></td>
       <% else %>
diff --git a/app/views/wishes/new.html.erb b/app/views/wishes/new.html.erb
deleted file mode 100644
index 7072928..0000000
--- a/app/views/wishes/new.html.erb
+++ /dev/null
@@ -1,19 +0,0 @@
-<% provide title: 'New Wish' %>
-
-<div class="row">
-  <div class="col-md-6 col-md-offset-3">
-    <%= form_for(@wish) do |f| %>
-      <%= f.label :title, 'Title' %>
-      <%= f.text_field :title, class: 'form-control' %><br>
-
-      <%= f.label :description, 'A longer description of your gift with any relevant details, including URLs, ISBNs, etc' %>
-      <%= f.text_area :description, cols: '10', rows: '10', class: 'form-control' %><br>
-
-      <%= f.hidden_field :friend_id, value: current_user.id %>
-      <%= f.hidden_field :year, value: Time.christmas_year %>
-
-      <%= f.submit 'Make wish', class: 'btn-btn-primary' %>
-      <%= link_to 'Cancel', :back %>
-    <% end %>
-  </div>
-</div>
diff --git a/app/views/wishes/show.html.erb b/app/views/wishes/show.html.erb
index d8fcb89..50c9f37 100644
--- a/app/views/wishes/show.html.erb
+++ b/app/views/wishes/show.html.erb
@@ -10,8 +10,7 @@
 
 <h3>Fulfilled with:</h3>
 <% if current_user == @wish.friend %>
-  <p>You'll just have to wait until Christmas to find out!</p><br>
-  <%= link_to 'Edit', edit_wish_path(@wish)%>
+<p>You'll just have to wait until Christmas!</p>
 <% elsif @wish.gifts.any? %>
   <ul>
     <% @wish.gifts.each do |gift| %>
diff --git a/config/routes.rb b/config/routes.rb
index 3419eef..4267e9d 100644
--- a/config/routes.rb
+++ b/config/routes.rb
@@ -6,7 +6,6 @@ Rails.application.routes.draw do
 
   get    '/login',   to: 'sessions#new', as: 'login'
   post   '/login',   to: 'sessions#create'
-  get    '/logout',  to: 'sessions#destroy'
   delete '/logout',  to: 'sessions#destroy'
 
   get '/help',             to: 'static_pages#help'
